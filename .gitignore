# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# API - user uploads must never be tracked (git pulls would wipe them).
api/uploads/

# API - local credentials.
# NOTE: api/config.local.php IS currently committed for deploy convenience —
# the repo is private and the user has chosen not to rotate. If you ever
# rotate creds, set them on the server only and re-add this file to the
# .gitignore on the same commit.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
